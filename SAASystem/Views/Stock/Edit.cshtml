@model SAASystem.Models.View.StockViewModel.EditViewModel
@{
    ViewData["Title"] = "Edit";
}
@section CSS {

    <link href="https://getbootstrap.com/docs/5.0/examples/features/features.css" rel="stylesheet">
}
<div class="px-4 py-5" id="custom-cards">
    <h2 class="pb-2 border-bottom">Edit</h2>
    <div class="g-4 py-5">
        <div class="row g-5">
            <div class="col-md-7 col-lg-8">
                <form class="needs-validation" form method="post" asp-controller="Stock" asp-action="Edit">
                    <div class="row g-3">
                        <div class="col-sm-12 mb-2">
                            <label asp-for="Form.StockId" class="form-label"></label>
                            <input asp-for="Form.StockId" class="form-control" readonly>
                            <span asp-validation-for="Form.StockId" class="text-danger"></span>
                        </div>
                        <div class="col-sm-12 mb-2">
                            <label asp-for="Form.ApartmentId" class="form-label"></label>
                            <select asp-for="Form.ApartmentId" asp-items="@Model.ApartmentEnumerable" class="form-control"></select>
                            <span asp-validation-for="Form.ApartmentId" class="text-danger"></span>
                        </div>
                        <div class="col-sm-12 mb-2">
                            <label asp-for="Form.Name" class="form-label"></label>
                            <input asp-for="Form.Name" class="form-control">
                            <span asp-validation-for="Form.Name" class="text-danger"></span>
                        </div>
                        <div class="col-sm-12 mb-2">
                            <label asp-for="Form.Status" class="form-label"></label>
                            <select asp-for="Form.Status" asp-items="@Model.StatusEnumerable" class="form-control"></select>
                            <span asp-validation-for="Form.Status" class="text-danger"></span>
                        </div>
                    </div>
                    <hr class="my-4">
                    <div class="row g-3">
                        <div class="col-sm-6">
                            <a asp-controller="Stock" asp-action="List" class="w-100 btn btn-secondary btn-sm">Back</a>
                        </div>
                        <div class="col-sm-6">
                            <button class="w-100 btn btn-primary btn-sm" type="submit">Save</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>